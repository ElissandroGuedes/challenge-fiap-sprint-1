<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Challenge FIAP"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChallengeFiap">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ChallengeFiap">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MessagesActivity"
            android:parentActivityName=".LoginActivity">
            <meta-data
                android:name="androidx.core.app.PARENT_ACTIVITY"
                android:value=".LoginActivity" />
        </activity>

        <activity
            android:name=".ContactsActivity"
            android:exported="false"
            android:parentActivityName=".MessagesActivity">
            <meta-data
                android:name="androidx.core.app.PARENT_ACTIVITY"
                android:value=".MessagesActivity" />
        </activity>

        <activity
            android:name=".ChatActivity"
            android:parentActivityName=".MessagesActivity">
            <meta-data
                android:name="androidx.core.app.PARENT_ACTIVITY"
                android:value=".MessagesActivity" />
        </activity>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".SendCampaignActivity" />
        <activity android:name=".CampaignDetailActivity" />

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
